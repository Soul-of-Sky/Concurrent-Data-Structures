lock-free hashset:
    init add args(max_num_cluster)

heap: 
    ukey_t -> entry_t

add 3 gcs
add notes
add asserts

test.c: 
    define -> avgs
    num_threads
    n
add Makefile.inc
fix warnings
change init maybe
maybe remove static global vals